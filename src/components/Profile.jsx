import React from 'react';
import { useContext } from 'react';
import { UserContext } from '../Providers/AuthProviders';

const Profile = () => {
    return (
        <div>
            <h3>This is  a user Profiel</h3>
        </div>
    );
};

export default Profile;